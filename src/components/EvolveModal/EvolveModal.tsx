import styles from "./EvolveModal.module.scss";

type Props = {};

const EvolveModal: React.FC<Props> = (props: Props) => {
    return <div className={styles["evolve-modal"]}>Evolve</div>;
};

export default EvolveModal;
