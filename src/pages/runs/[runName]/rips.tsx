import type { NextPage } from "next";
import Head from "next/head";

const RIPs: NextPage = () => {
    return (
        <>
            <Head>
                <title>RIPs</title>
            </Head>
        </>
    );
};

export default RIPs;
